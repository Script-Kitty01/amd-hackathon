# Runtime dependency: OpenAI-compatible client used for both the Fireworks
# fallback and the local LLM endpoint (Ollama exposes an OpenAI-compatible API).
openai>=1.40.0

# (dev only, not shipped in the image: pytest)
